{
  "name": "webshop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ci": "npm run test:run && npm run e2e:headless",
    "e2e": "cypress open --e2e",
    "e2e:headless": "cypress run --e2e"
  },
  "dependencies": {
    "firebase": "^12.4.0",
    "lucide-vue-next": "^0.545.0",
    "vue": "^3.5.22",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "@vitest/ui": "^4.0.18",
    "cypress": "^15.10.0",
    "happy-dom": "^20.6.1",
    "vite": "^7.1.7",
    "vitest": "^4.0.18",
    "wait-on": "^8.0.1"
  }
}
